apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: resource-broker-system

resources:
- deployment.yaml
- service.yaml
- referencegrant.yaml

images:
- name: resource-broker-portal
  newName: resource-broker-portal
  newTag: platform-mesh
